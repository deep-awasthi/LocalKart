package com.localkart.platform.delivery.domain;

/**
 * Enumeration of all possible delivery tracking states.
 */
public enum DeliveryStatus {
    PENDING,
    ASSIGNED,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
